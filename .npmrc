# Hoist all dependencies to root for monorepo plugin architecture
# This is required for dynamic plugin loading with ES modules
shamefully-hoist=true

# Prevent hoisting build scripts for security
ignore-scripts=false
