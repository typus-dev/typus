// pageConfiguration.ts
import { dashboardConfig } from './dashboard.dsx';
import { sessionsConfig } from './sessions.dsx';
import { loginHistoryConfig } from './loginHistory.dsx';

export const pageConfiguration = {
  dashboard: dashboardConfig,
  sessions: sessionsConfig,
  loginHistory: loginHistoryConfig
};